# No server-side environment variables are required.
# Users paste their own Groq API key into the Settings modal in the UI.
# The key is stored in localStorage and sent per-request as the
# `x-groq-api-key` header.
